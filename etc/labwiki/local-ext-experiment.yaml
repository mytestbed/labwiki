
labwiki:
  repositories:
    default:
      type: git
      top_dir: ~/tmp/lw_repo
      is_primary: true

  plugins:
    experiment:
      plugin_dir: labwiki_experiment_plugin
      artifact_dir: git:default:/experiments  # Where to store experiment related artifacts
      ec_runner: omf_exec/omf_exec-norbit.sh
      oml:
        host: localhost
        user: max
        #pwd: omlisgoodforyou
